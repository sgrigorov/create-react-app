import React from "react";

function Header () {
    return (
        <section>
            <hr className="header-top__separator" />
            Stan's Header
            <hr className="header-top__separator" />
        </section>
    )
}

export default Header;
